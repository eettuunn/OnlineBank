После развертывания сервисов с помощью докера (на данный момент core-service на порту 8080)
необходимо перейти в папку /gateway и написать следующие команды:

npm install
npm run start

Теперь core-service доступен по адресу http://127.0.0.1:3000/core-service/
